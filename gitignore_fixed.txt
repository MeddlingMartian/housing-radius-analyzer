# =============================================================================
# .gitignore — Housing Radius Analyzer
# IMPORTANT: Git does NOT support inline comments after a pattern.
# Only lines that BEGIN with # are treated as comments.
# =============================================================================

# -----------------------------------------------------------------------------
# CRITICAL: API keys and secrets
# config.json holds live keys — commit config.template.json instead.
# -----------------------------------------------------------------------------
config.json
*.env
.env.*
secrets.mat
*_credentials.*
*_apikey.*
keyvault_local.json

# -----------------------------------------------------------------------------
# MATLAB generated files
# -----------------------------------------------------------------------------
*.asv
*.m~
*.mexw32
*.mexw64
*.mexa64
*.mexmaci64
slprj/
codegen/
*.mlx.bak
.matlab/

# -----------------------------------------------------------------------------
# Cache and pipeline output
# data/ subdirectories are managed by the Azure pipeline on the
# data/housing-pulls branch. The .gitkeep placeholder IS tracked.
# Negation (!data/.gitkeep) must NOT follow a directory wildcard —
# Git cannot un-ignore a file inside an already-ignored directory.
# Use named sub-patterns instead (safe and explicit):
# -----------------------------------------------------------------------------
.hra_cache/
hra_cache/
pipeline_outputs/
data/20*/
data/pull_*/
*_stub.mat
hra_last_pull.mat

# -----------------------------------------------------------------------------
# Downloaded Census/HUD data files (large binaries regenerated by pipeline)
# -----------------------------------------------------------------------------
*.gz
*.tar
*.csv.gz
pums_*.csv
csv_h*.zip
psam_h*.csv

# -----------------------------------------------------------------------------
# Exported output files (always regenerated — not source-controlled)
# -----------------------------------------------------------------------------
*.xlsx
output.json
output.xml
output.csv

# -----------------------------------------------------------------------------
# ZIP archives (stored in Azure Blob, not Git)
# Exception: allow the installer bootstrap zip if present.
# -----------------------------------------------------------------------------
*.zip
!install/bootstrap.zip

# -----------------------------------------------------------------------------
# macOS
# -----------------------------------------------------------------------------
.DS_Store
._*
.Spotlight-V100
.Trashes

# -----------------------------------------------------------------------------
# Windows
# -----------------------------------------------------------------------------
ehthumbs.db
Thumbs.db
desktop.ini

# -----------------------------------------------------------------------------
# Linux swap files
# -----------------------------------------------------------------------------
*~

# -----------------------------------------------------------------------------
# IDE and editor folders
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
.history/

# -----------------------------------------------------------------------------
# Azure DevOps / CI build artifacts
# -----------------------------------------------------------------------------
drop/
dist/
bin/
obj/
TestResults/

# -----------------------------------------------------------------------------
# Logs
# -----------------------------------------------------------------------------
*.log
logs/
npm-debug.log*

# -----------------------------------------------------------------------------
# Node / Jekyll (GitHub Pages toolchain only)
# -----------------------------------------------------------------------------
node_modules/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
_site/
